* Pimeiro passo foi fazer a instalação do docker-compose:
* *  sudo chmod +x /usr/local/bin/docker-compose
* * * Em seguida foi dada a permissão com o seguinte comando: 
* * sudo chmod +x /usr/local/bin/docker-compose
* 

* Depois foi feito a verificação da instalação com o comando:
* docker-compose --version
* * Em seguida foi criada um arquivo chamado Docker-compose.yml:
* version: '3.1'
* * services:
* *  wordpress:
*     image: wordpress
*    container_name: wordpress
*    restart: always
*    ports:
*      - 80:80
*    environment:
*      - WORDPRESS_DB_HOST: database-1.cqj4vnmav8pj.us-east-1.rds.amazonaws.com
*      - WORDPRESS_DB_USER: admin
*      - WORDPRESS_DB_PASSWORD: morais99
*      - WORDPRESS_DB_NAME: wordpress
*    volumes:
*      - /home/joao/wordpress:/var/www/html


* Depois executei o Docker-compose com o comando:
* sudo docker-compose up -d
     
* foi feito um script com o comandos a seguir:
* #!/bin/bash
* * # Atualizar o sistema
* sudo yum update -y
* * # Instalar o Docker
* sudo amazon-linux-extras install docker -y
* sudo service docker start
* sudo usermod -aG docker ec2-user
* * # Habilitar o Docker para iniciar na inicialização
* sudo chkconfig docker on
* * # Montar o EFS (substitua <EFS_ID> e <REGION>)
* sudo yum install -y nfs-utils
* sudo mkdir /mnt/efs
* sudo mount -t nfs4 -o nfsvers=4.1,rsize=1048576,wsize=1048576,hard,timeo=600,retrans=2 <EFS_ID>.efs.<REGION>.amazonaws.com:/ /mnt/efs
* * Faltando configurar o serviço de wordpress, Load Balance e EFS.